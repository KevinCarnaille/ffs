import {Injectable} from '@angular/core';import { Http, Response } from '@angular/http';import 'rxjs/add/operator/map';import { Subject } from 'rxjs/Subject';import {BehaviorSubject} from "rxjs";import {CommentInterface} from "./comments.interface";@Injectable()export class AddCommentService {    // Observable string sources    private newComment = new Subject<CommentInterface>();    // Observable string streams    newComment$ = this.newComment.asObservable();    addComment(comment: CommentInterface) {        return this.newComment.next(comment);    }}@Injectable()export class AnswerCommentService {    // Observable string sources    private answer = new Subject<number>();    // Observable string streams    answer$ = this.answer.asObservable();    addAnswer(id: number) {        return this.answer.next(id);    }}